machine:
  node:
    version: 6.11.1
  ruby:
    version: 2.3.1
  
dependencies:
  pre:
    - gem install bundler

checkout:
  post:
    - bundle install

test:
  override:
    - echo "No tests"

deployment:
  production:
    branch: master
    commands: 
      - mkdir _comments
      - rake disquscomments
      - JEKYLL_ENV=production bundle exec jekyll build
      - ls